name: Deploy

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  setup:
    uses: ./.github/workflows/setup.yml

  test:
    steps:
      - name: Test
        run: make test
