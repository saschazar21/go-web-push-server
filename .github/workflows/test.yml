name: Deploy

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  setup:
    uses: ./.github/workflows/setup.yml

  test:
    name: Run unit tests
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: make test
